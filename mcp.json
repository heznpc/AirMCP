{
  "mcpServers": {
    "airmcp": {
      "command": "npx",
      "args": [
        "-y",
        "airmcp"
      ],
      "description": "Approval-gated local action runtime for Apple workspaces — 289 tools across 29 modules. macOS only.",
      "env": {}
    }
  },
  "version": "2.14.0"
}
